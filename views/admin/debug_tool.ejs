<script>
    
function debugOutputAllArray() {
    debugOutputMarkerArray(MARKERS);
    debugOutputMarkerArray(CLICKMARKER);
    debugOutputMarkerArray(USERMARKER);

    debugOutputWindowArray(MARKERWINDOWINFOS);
    debugOutputWindowArray(CLICKWINDOWINFO);
    debugOutputWindowArray(MARKERTYPE);
}

function debugOutputWindowArray(type) {
    console.log("Start Debug >> ");
    if(type == MARKERWINDOWINFOS) 
        console.log("Array type [MARKERWINDOWINFOS] , length [", markerWindowinfos.length, "]");
    else if(type == CLICKWINDOWINFO)
        console.log("Array type [CLICKWINDOWINFO] , length [", clickWindowInfo.length, "]");
    else if(type == MARKERTYPE)
        console.log("Array type [MARKERTYPE] , length [", markerType.length, "]");
    else {
        console.log("<< End Debug [wrong type] ");
        return;
    }
    container = getContainer(type);

    for (let i=0; i<container.length; i++) {
        console.log("[", i, "] =>", container[i]);
    }

    console.log("<< End Debug");
}

function debugOutputMarkerArray(type) {
    console.log("Start Debug >> ");
    if(type == MARKERS) 
        console.log("Array type [MARKERS] , length [", markers.length, "]");
    else if(type == CLICKMARKER)
        console.log("Array type [CLICKMARKER] , length [", clickMarker.length, "]");
    else if(type == USERMARKER)
        console.log("Array type [USERMARKER] , length [", userMarker.length, "]");
    else {
        console.log("<< End Debug [wrong type] ");
        return;
    }
    container = getContainer(type);

    for (let i=0; i<container.length; i++) {
        console.log("[", i, "] =>", container[i]);
    }

    console.log("<< End Debug");
}

</script>