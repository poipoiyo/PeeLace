<script>

function handleError(errFunc, errCode) {
    var currentUrl = window.location.href;
    window.location.replace(currentUrl + "/server-error");
    console.log("[" + errFunc + "] :" + getErrorMsg(errCode));
}

function getErrorMsg(code) {
    return ERROR_MSG[code];
}

</script>